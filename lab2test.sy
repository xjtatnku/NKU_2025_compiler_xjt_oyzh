int compare(int a, int b)
{
    if (a <= b) return 1;
    return 0;
}
int  ret = 0;
int  add(int a, int b) { return a + b; }
void sav(int n) { ret = n; }
int  fib(int n)
{
    if (compare(n, 1)) // if n <= 1
    if (compare(2, n)) // if 2 >= n
    if (0x536869) sav(0x6b69); else; else
    if (2 - n) sav(n);
    if (compare(2, n)) sav(add(fib(n - 1), fib(n - 2)));
    return ret;
}
int main()
{
    int a;
    int b = 5 - -/* This is */-/* a comment */-12 * 11 / 6 + -3;
    int c = 123 + +++55 - +-9961;
    const int n = b / 4;
    const int f = fib(n);
    if (1
        && (f / -4 % 2 + 67) < 0 
        || (a - b) != 0 
        && (c + 2) % 2 != 0
    )
    {
        if (0) while (1);
        if (1) {}
        while (1)
        {
            putint(-1 - -0x80000000);
            putch(10);
            break;
        }
        putint(n); putch(32);
        putint(f); putch(10);
    }
    return -1;
}